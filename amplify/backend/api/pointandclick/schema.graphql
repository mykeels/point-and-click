type Todo @model(timestamps:{createdAt: "createdAt", updatedAt: "updatedAt"}) {
  id: ID!
  x: Float!
  y: Float!
  user: String
  color: String
}

type LoginEvent @model(timestamps:{createdAt: "createdAt", updatedAt: "updatedAt"}) {
  id: ID!
  user_id: String!
}